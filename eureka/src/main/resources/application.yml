# 服务端口
server:
  port: 8000


# eureka配置
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost


# 指定生效的配置文件
spring:
  profiles:
    active: native

logging:
  config: classpath:logback-spring.xml